// This file is overwritten by the Netlify build command with actual values.
// Fallback for local development or if build hasn't run yet.
window.NETLIFY_CONFIG = window.NETLIFY_CONFIG || { clientId: '', tenantId: '' };
